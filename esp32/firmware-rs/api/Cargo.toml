[package]
name = "fluxchess-api"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", default-features = false, features = ["derive"] }
ts-rs = { version = "10", optional = true }

[features]
default = []
ts = ["ts-rs", "serde/std"]
